// Kinetra v0.0.1a22 string demo

let greeting = "Hello";
let name = "Kinetra";

print greeting + ", " + name + "!";
print len(name);
print greeting == "Hello";
print greeting != name;

let words = ["alpha", "beta"];
print words[0];
print len(words[0]);

print "tab:\there\nnewline above";