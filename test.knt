// Kinetra v0.0.1a07 test

let a = vec3(1.0, 2.0, 3.0);
let b = vec3(4.0, 5.0, 6.0);

print dot(a, b);
print cross(a, b);
print length(a);
print normalize(b);

let p = vec3(0.0, 0.0, 0.0);
let target = vec3(3.0, 4.0, 0.0);

sim 2 {
	p = p + normalize(target) * dt;
	print p;
}