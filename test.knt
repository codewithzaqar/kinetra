// Kinetra v0.0.1a06 test

let a = vec3(1.0, 2.0, 3.0);
let b = vec3(4.0, 5.0, 6.0);

let c = a + b;

print c;

let p = vec3(0.0, 0.0, 0.0);
let v = vec3(1.0, 0.0, 0.0);

sim 3 {
	p = p + v * dt;
	print p;
}